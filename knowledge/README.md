# CryptoRadar Knowledge Folder

Put local crypto knowledge files here. Supported formats are PDF, TXT, MD, CSV, JSON, and DOCX.

Run:

```bash
python main.py --rebuild-knowledge
```

The bot indexes these files locally and retrieves relevant chunks during signal analysis. Sources are not sent to cloud APIs by default.
